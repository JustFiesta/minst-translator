[project]
name = "pjm-translator"
version = "0.1.0"
description = "Real time translator from PJM to Polish native language"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.4",
]
